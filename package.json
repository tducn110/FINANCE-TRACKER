{
  "name": "finance-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix client && npm install --prefix server && cd client && npm run build && cd ../server && npm run build"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
